# v0.4 — LCD display

- **Status:** finished
- **Target:** v0.4
- **Updated:** 2026-07-17

## Goal
Drive the G13's 160x43 monochrome LCD: show active profile and basic system info.

## Tasks
- [x] Reverse-engineer / implement the LCD framebuffer write (USB control/bulk transfer).
- [x] 1-bit framebuffer abstraction (160x43) + simple text rendering.
- [x] Render active profile name.
- [ ] Optional CPU/clock readout (deferred — out of scope for v0.4).

## Acceptance
Text appears on the physical LCD and updates on profile switch.

## Hardware smoke test (manual — no unit tests on the USB transfer) — PASSED 2026-07-17
- [x] Text renders on the physical LCD (init transfer + endpoint 0x02 confirmed).
- [x] Title + mode box show; mode reflects Active vs Dry-run.
- [x] Profile line shows the active profile name (poller-driven, same path as switching).
- [x] Last-action line updates on a G-key press and persists.
- [x] GUI preview is pixel-exact with the physical panel.
- [~] Long-name truncation / replug re-send / no-device: covered by the reviewed
      code paths + the RGB feature's verified reconnect/no-device behavior (same
      supervisor); not each independently re-exercised this session.

## Notes from bring-up
- `init_lcd` (SET_CONFIGURATION-style control transfer) + `write_interrupt` to
  endpoint 0x02 both work on real hardware first try — the panel showed the HUD.
- The pixel-exact GUI preview matched the physical panel, confirming the
  framebuffer/font/layout independently of the USB write.
- Gotcha: the release build loads `target/release/config.toml` (next to the exe),
  not the repo-root one.
